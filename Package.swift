// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "CSelect",
    products: [
        .library(
            name: "Cselect",
            targets: ["Cselect"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "Cselect",
            dependencies: []),
    ]
)
